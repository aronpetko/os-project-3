5/8/2026 4:15PM: I have added the argument parsing. `create` also writes a valid header. `open_existing` validates the magic number; other commands stub out with a clean error. No B-tree yet for the actual indexing of the file. I need to implement the B-Tree and implement a `Node` structure with I/O interactions. I also need to implement `insert` and `search`.

5/8/2026 4:39PM: Now it has a basic insert/search. There is no splitting with the nodes, like a proper B-Tree. I'm definitely going to need some more assistance on how to properly handle that. B-Tree's are very complicated.

5/8/2026 7:35PM: I spent the last 3 hours working on handling inserts with multiple splits, `print` now works in order properly, and `search` also works. Everything else is more polished as well and the project is in its complete state. Seems to work with the `test.idx` file provided as well.